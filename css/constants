// This is the background color for major blocks in the interface.
// bar_and_box_background = #CCC
bar_and_box_background = #DFDFDF
bar_and_box_border = #CCC

primary_light = #7fa2f1
primary_dark  = #63a61b

// CleverCSS seems to have a parsing error with URLs containing a colon in
// function calls. For now, we need to top-level domains in variables to work
// around the parser's defect. This actually works out pretty well for us so
// that we only have to make one change if we ever change the image host.
images = "http://jekor.com/"
